apiVersion: apps/v1
kind: Deployment
metadata:
  name: founderbrain-backend
  namespace: founderbrain
spec:
  replicas: {{ .Values.backend.replicas }}
  selector:
    matchLabels:
      app: founderbrain-backend
  template:
    metadata:
      labels:
        app: founderbrain-backend
    spec:
      containers:
        - name: backend
          image: {{ .Values.backend.image }}
          ports:
            - containerPort: 8080
          env:
            - name: DB_URL
              valueFrom:
                secretKeyRef:
                  name: founderbrain-secrets
                  key: DB_URL
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: founderbrain-secrets
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: founderbrain-secrets
                  key: DB_PASSWORD
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: founderbrain-secrets
                  key: JWT_SECRET
            - name: ML_SERVICE_URL
              value: "http://founderbrain-ml:8000"
            - name: PORT
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: founderbrain-backend
  namespace: founderbrain
spec:
  selector:
    app: founderbrain-backend
  ports:
    - port: 8080
      targetPort: 8080
  type: LoadBalancer
